# 仕様書（Claude Code向け・技術仕様）v0.6

対象プロジェクト: 広島県企業向け AI離職予防・人材定着支援サービス
最終更新: 2026-08-25（v0.5からの変更: TAフィードバック対応状況を反映、2日後ミーティングに向けた優先順位確定、本番プレゼン仕様を追加）

---

## ⚠️ v0.6での重要な決定事項

- **2日後（8/27予定）にTAとのミーティングがあり、そこで新たな改善案をもらう予定。** そのため「完璧」ではなく「**安定して動くデモ＋主要指摘への対応実績**」を優先する。
- **8/26以降のコード変更は、下記「優先実装リスト」のみに限定する。** それ以外の新規提案（適性診断・チャットツール連携等）は今回は実装せず、プレゼンでの言及・`docs/future_scope.md`への記録に留める。
- **本番プレゼン仕様が確定：発表7分＋質問対応7分（質問3問想定）。** スライド枚数は7分に収まれば自由、目安は1枚1分（＝7枚程度）。

---

## ✅ TAフィードバック対応状況（8/25時点）

TAとのミーティングで複数の改善案をもらい、実装可否を仕分けた。

| # | 項目 | ステータス | 対応方針 |
|---|---|---|---|
| 1 | 英語表記の日本語化（ラベル・カラム名） | **実装済み** | 日本語マッピング辞書で表示層のみ変換 |
| 2 | ドル→日本円への換算表示 | **実装済み** | 表示用フォーマット関数で対応、元データは変更なし |
| 3 | 原因分析パートの視覚化（文字だらけ問題） | **優先実装（未着手）** | 個人の特徴量 vs 全体平均を横棒グラフ等で表示。AI画像生成は不採用（外部API依存・不安定要因のため見送り） |
| 4 | 欠損値のある入力データへの対応 | **実装見送り** | 自動補完（imputation）も検討したが、モデル予測の信頼性に関わり拙速な実装はデモ崩壊リスクがあるため今回は見送り。方針はプレゼンで説明 |
| 5 | データ入力方法のわかりやすさ改善 | **優先実装（未着手）** | サンプルCSVダウンロードボタン＋入力項目の日本語説明を追加（#1と組み合わせやすい） |
| 6 | 適性診断機能の追加提案 | **実装見送り** | 凍結タスク（総合プロフィール分析）の「性質・特質」領域と統合し、`future_scope.md`の構想に反映 |
| 7 | 社内チャットツール（Slack等）との連携 | **実装見送り（提案時から明言済み）** | プレゼンの「将来構想」で言及のみ |
| 8 | 広島データ／入力データの精度に関する指摘 | **実装見送り** | 「日本企業は現行データセットほど精緻な人事データを持っていない」という前提の見直し。#6, #7と統合し、「チャットログ等の既存データを代替指標として活用する」という将来ビジョンとして`future_scope.md`に一本化 |
| 9 | UI/UXデザインの変更（参考サイトあり） | **方針未定** | ユーザーが参考サイトを自身で調査中。着手判断は保留 |
| 10 | 色の組み合わせ | プレゼン資料のみで対応 | アプリのUIとは別、PowerPointの配色調整のみ |

---

## 🎯 残り2日間の優先実装リスト（Claude Codeへの依頼事項）

1. **原因分析の視覚化**：`explain_risk_factors()`の出力に、個人の特徴量と全体平均を比較する横棒グラフ（matplotlib/plotly）を追加。文章の説明と併記する形。
2. **サンプルCSV＋入力説明の追加**：ダウンロード用のサンプルCSVを`data/`配下に用意し、UI上に「まずこの形式で試してください」という導線を追加。各列の意味を日本語で簡潔に説明。

上記2点の実装後、**デモ3パターン（高#622／中#1203／低#1804）が問題なく動くことを必ず確認する。**

---

## 実装済み（変更なし・現状維持）

以下はすべて実装・動作確認済み。**新規タスクではリグレッションに注意すること。**

### 技術スタック
- 言語: Python（全体）
- MLモデル: LightGBM（`class_weight="balanced"`、判定閾値 0.30）
- UI: Streamlit（フロント・バック共にPython、分離なし）
- LLM: Claude API（開発時 Haiku 4.5 / デモ本番 Sonnet 5、UIトグルで切替）
- データセット: IBM HR Analytics Employee Attrition（Kaggle公開データ）

### モデル評価指標
- Accuracy: 0.840 / ROC-AUC: 0.798
- Recall（離職クラス）: 0.49（class_weight="balanced"適用後、閾値0.30で運用）
- Feature importance上位: MonthlyIncome, DailyRate, Age, DistanceFromHome, MonthlyRate

### ファイル構成
```
hackathon_project/
├── app.py                        # Streamlitメインアプリ
├── src/
│   ├── model.py                  # transform_with_encoders(), validate_dataset(), friendly_data_error_message()
│   └── llm.py                    # explain_risk_factors(), suggest_interventions()
├── data/
│   ├── WA_Fn-UseC_-HR-Employee-Attrition.csv
│   ├── company_policies.txt      # 簡易RAG用（6項目）
│   └── sample_input_template.csv # 入力サンプルCSV（v0.6で新規追加予定）
├── models/
│   ├── lgbm_model.pkl
│   ├── encoders.pkl
│   └── feature_names.pkl
├── design/
│   └── dashboard_reference.html  # Claude Designで作成した参考デザイン（不採用・アーカイブ扱い）
└── docs/
    └── future_scope.md           # 将来構想の構想メモ（総合プロフィール分析／適性診断／チャットツール連携／代替データソースを統合予定）
```

### 実装済み機能（MVP 5ステップ）
1. CSVアップロード・従業員選択（デモ用ショートカット: 高#622/中#1203/低#1804）
2. 離職リスクスコア表示（%、閾値0.30に対する判定）
3. Feature importance上位5件表示
4. `explain_risk_factors()`：原因分析（自然文、日本語ラベル・円換算対応済み）
5. `suggest_interventions()`：簡易RAGベースの施策提案

### エラーハンドリング（8/23実装済み）
- API認証失敗・タイムアウト → `friendly_error_message()` でユーザー向けメッセージに変換
- CSV異常データ（欠損値・列欠落・型混入） → `validate_dataset()` + `friendly_data_error_message()` で保護（※自動補完は行わず、エラーとして通知する現行方式を維持）
- `st.cache_resource`（モデル）/ `st.cache_data`（データ・LLM結果）でrerun時の重い処理再実行を回避
- 従業員ID別にsession_stateを管理し、切替時に古い分析結果が残らないようにしている

---

## 凍結タスク（将来構想として統合・実装見送り確定）

複数のTA提案（総合プロフィール分析、適性診断、チャットツール連携、データ制約への対応）を、**「データ取得のハードルを下げつつ、従業員を多面的に見る」という一つの将来ビジョンとして統合する。**

### 統合ストーリー（`docs/future_scope.md`記載予定）
1. **現状の課題**：本MVPはIBM HR Analytics（海外企業の詳細な人事データ）を使用しているが、日本企業の多くはここまで精緻な人事データを保有していない。
2. **代替データソースの方向性**：Slack等の社内チャットツールは日本企業でも導入率が高く、「新たに詳細データを集める」のではなく「既に存在するデータ（チャットログの頻度・トーン変化等）を使う」設計へ転換できる可能性がある。
3. **総合プロフィール分析への発展**：上記データを活用し、離職リスクという単一指標だけでなく、成長曲線・強み弱み・性質特質（適性診断的な要素を含む）を可視化する。

### 提案内容（技術的記録）
1. **成長曲線**（Growth Trajectory）：`YearsAtCompany`, `YearsSinceLastPromotion`等から成長ペースを可視化
2. **強み・弱み**（Strengths / Weaknesses）：個人の特徴量と全体平均を比較し、LLMで自然文化
3. **性質・特質**（Traits／適性診断的要素）：`JobSatisfaction`, `WorkLifeBalance`, `EnvironmentSatisfaction`等の満足度系カラムをLLMで要約

### 実装方針（着手する場合の設計案・参考記録）
- 既存のMVP機能は変更しない
- 新規関数案: `src/llm.py` に `analyze_employee_profile()` を追加（1回のLLM呼び出しでまとめて生成）
- UIには「総合プロフィール分析」ボタンを追加

### 確定した対応
- **コード実装は行わない。**
- `docs/future_scope.md` に統合ストーリーとして記録し、プレゼンの「将来構想」スライドの参考資料とする。
- デモでは見せない。口頭・スライド上の言及に留める。

---

## 🎤 本番プレゼン仕様（v0.6で新規確定）

| 項目 | 内容 |
|---|---|
| 発表時間 | 7分 |
| 質問対応時間 | 7分（**質問3問を想定**） |
| スライド枚数 | 自由（7分に収まること）。目安は1枚1分＝7枚程度 |

### 想定スライド構成（目安7枚）
1. 表紙
2. 課題（データ＋人間ドラマ、左右分割）
3. INSIGHT（離職ゼロは困難、だから予兆検知に注力）
4. 解決策
5. デモ
6. ビジネスモデル図
7. 締め＋CTA（将来構想への言及もここ or INSIGHT内で）

### 質問対応の準備（未着手）
3問想定のため、想定質問集（FAQ）の事前準備が必要。現時点で会話から見えている弱点候補：
- Recall 0.49という数値の実用性について
- 海外データセット（IBM HR）の日本企業への適用可能性
- 個人情報・プライバシーの取り扱い

---

## 残りスケジュール（v0.6更新）

| 日付 | フェーズ | Claude Codeへの依頼事項 |
|------|---------|------------------------|
| 8/25-26 | **優先実装2点＋デモ確認** | 原因分析の視覚化、サンプルCSV＋入力説明の追加。実装後は高・中・低3パターンのリグレッション確認必須 |
| 8/27 | **TAとのミーティング** | 新たな改善案を受領予定。ここまでに「動くデモ＋主要指摘への対応実績」を用意する |
| 8/27-28 | プレゼン資料最終化・台本作成（7分＋FAQ準備） | コード変更なし想定。`docs/future_scope.md` 作成、ミーティング後の追加対応があれば都度判断 |
| 8/29 | リハーサル | デモ動作確認（バグがあれば最小修正で対応） |
| 8/30 | 予備日 | 直前バグ修正のバッファ |
| 8/31 | 提出 | — |

---

## 注意事項（Claude Codeへの申し送り）
- ユーザーはPython中心の学習歴で、フロントエンド（HTML/CSS/JavaScript）は基礎理解のみ。技術説明は平易に。
- Streamlit の制約（HTML/CSS直接編集不可、rerunで上書きされる）は理解済みなので、再度の説明は不要。
- **8/26以降は「優先実装リスト」の2点のみに限定してコード対応する。** それ以外の新規提案（適性診断・チャットツール連携・UI/UXデザイン変更・欠損値自動補完等）は、着手前に必ずユーザーに確認する。
- デモの安定性（高・中・低リスク3パターンが正常動作すること）が最優先。これを壊すリスクのある変更は行わない。
- 8/27にTAミーティングを控えているため、**「完璧」より「安定動作＋主要指摘への対応実績」を優先する**方針であることを踏まえて対応すること。
