# 仕様書（Claude Code向け・技術仕様）v0.7

対象プロジェクト: 広島県企業向け AI離職予防・人材定着支援サービス
最終更新: 2026-08-26（v0.6からの変更: 運営からの正式プレゼン仕様・提出物リストを反映、UI修正の対応状況を更新）

---

## ⚠️ v0.7での重要な決定事項

- **運営から正式な発表仕様が通知された。** プレゼン7分＋質疑応答8分（質問3問想定）＝持ち時間合計15分。
- **8/31までの提出物が4種類、新たに判明した。** これまで想定していた「アプリ＋プレゼン資料」だけでなく、デモ動画・技術文書の準備が必要。
- **UI修正（前回依頼分）は一部完了、一部は新たな指摘により方針転換。** 詳細は下記「UI修正の対応状況」参照。

---

## 🎤 本番プレゼン仕様（v0.7で運営から正式通知・確定）

| 項目 | 内容 |
|---|---|
| 発表時間 | 7分 |
| 質疑応答時間 | **8分（質問3問を想定）** |
| 持ち時間合計 | **15分** |
| スライド枚数 | 自由（7分に収まること）。目安は1枚1分＝7枚程度 |

---

## 📦 8/31までの提出物一覧（v0.7で新規判明）

| # | 提出物 | ステータス | 備考 |
|---|---|---|---|
| 1 | プレゼン資料（PDF） | 作成中 | PowerPointから書き出しで対応可 |
| 2 | **デモ動画（3分以内）** | **未着手** | 高・中・低リスク3パターンをどう3分に収めるか、台本設計が必要 |
| 3 | デモURLまたは実行方法 | 未着手 | ローカル環境の実行手順を簡潔に文書化する（README的なもの） |
| 4 | **サービス概要** | 未着手 | 文書化（想定：A4 1枚程度のサマリー） |
| 5 | **使用したAI技術一覧** | 未着手 | 技術スタックの一覧化（LightGBM、Claude API等） |

### Claude Codeへの依頼事項（提出物関連）
- #3（デモURLまたは実行方法）：README.md相当のセットアップ手順書を作成できるか確認する。既存のファイル構成・技術スタック情報を流用すればコード変更なしで対応可能な想定。
- #5（使用したAI技術一覧）：既存の技術スタック情報（LightGBM、Streamlit、Claude API等）を一覧化するだけなので、コード変更は不要。ドキュメント作成のみ。
- #2（デモ動画）、#4（サービス概要）はコード作業ではなく企画・文書作成が中心のため、Claude Codeへの依頼は基本的になし。ただし動画撮影のためにデモがスムーズに動く状態を維持することが前提条件になる。

---

## ✅ UI修正の対応状況（v0.7時点）

### 完了した対応
| # | 項目 | 状態 |
|---|---|---|
| 1 | 英語表記の日本語化（ラベル・カラム名） | ✅ 実装済み |
| 2 | ドル→日本円への換算表示 | ✅ 実装済み |
| 3 | カラーパレットの統一（部署別リスクヒートマップ） | ✅ 実装済み（緑→黄→赤の統一配色に変更確認済み） |
| 4 | ラベル表記バグ（「(USD)」残存）の修正 | ✅ 解消確認済み |
| 5 | AI生成文章内の英語カラム名残存 | ✅ 解消確認済み（`MonthlyIncome`等の表記は消えた） |

### 新規対応が必要な項目（v0.7で追加）
| # | 項目 | 内容 | 優先度 |
|---|---|---|---|
| 6 | **寄与度チャートの削除** | 「AIによる提案」タブの寄与度チャート（横棒グラフ）は、単位が抽象的（-200〜200のスコア）でドメイン知識のない人には理解しにくい。同内容が「個別詳細カルテ」タブの「主要リスク要因の詳細」表で既により分かりやすく表現されているため、チャートは削除し表に一本化する。凡例テキストも合わせて削除。 | 中（実施決定済み） |
| 7 | **施策提案文のMarkdown表示バグ修正** | 「定着施策の提案」実行時、生成文章内の太字記号「**」がそのまま表示されてしまう。Markdownが正しくレンダリングされるよう修正する（`st.markdown()`等の使用を確認）。「原因分析」の文章表示でも同様の問題がないか確認する。 | 中（実施決定済み） |

### Claude Codeへの依頼文（#6・#7、確定版）
```
【対応内容】① 寄与度チャートの削除　② 施策提案文のMarkdown表示バグ修正

【背景・課題】

① 寄与度チャートの削除
「AIによる提案」タブの原因分析セクションにある寄与度チャート（横棒グラフ、
横軸が-200〜200のスコア）は、単位が抽象的でドメイン知識のない人には
理解しにくい。また、同じ内容が「個別詳細カルテ」タブの「主要リスク要因の
詳細」表（実際の値・平均との比較%・色分け付き）で既により分かりやすく
表現されており、情報が重複している。
→ 表の方が直感的なため、寄与度チャートは削除し、表に一本化する。

② 施策提案文のMarkdown表示バグ
「定着施策の提案」を実行すると、生成された文章内に太字を示す
「**」記号がそのまま表示されてしまっている
（例：「**直ちに上長による1on1ミーティングを実施し...」）。
本来は太字でレンダリングされるべき箇所が、記号がそのまま見えてしまっている。

【対応方針】

① 寄与度チャートの削除
- 「AIによる提案」タブ内の寄与度チャート（横棒グラフ）の表示コードを削除する
- 併せて、グラフ上部にあった凡例テキスト（「赤：離職リスクを高めている要因／
  緑：定着に寄与している要因」）も削除する
- チャート生成に使っていたロジック・データ自体は他で使っていなければ削除、
  他機能で使用中であれば表示部分のみコメントアウトでも可
- 「主要リスク要因の詳細」表（個別詳細カルテタブ）は変更しない

② Markdown表示バグの修正
- LLMが生成した施策提案の文章を画面に表示する箇所で、Markdown記法
  （太字の**等）が正しくレンダリングされるように修正する
  （Streamlitの場合、st.write()やst.markdown()を使っているか確認し、
  適切な表示方法に統一する）
- 「原因分析」の文章表示でも同様の問題がないか、念のため確認する

【注意事項】
- 既存のリスク判定ロジックやスコア計算には一切触れない
- 変更後、高・中・低リスクの3デモパターン（#622 / #1203 / #1804）で
  表示崩れがないか必ず確認する
- 工数が想定より膨らみそうな場合は、その時点で一度立ち止まって相談する
```

---

## 実装済み（変更なし・現状維持）

以下はすべて実装・動作確認済み。**新規タスクではリグレッションに注意すること。**

### 技術スタック
- 言語: Python（全体）
- MLモデル: LightGBM（`class_weight="balanced"`、判定閾値 0.30）
- UI: Streamlit（フロント・バック共にPython、分離なし）
- LLM: Claude API（開発時 Haiku 4.5 / デモ本番 Sonnet 5、UIトグルで切替）
- データセット: IBM HR Analytics Employee Attrition（Kaggle公開データ）

### モデル評価指標
- Accuracy: 0.840 / ROC-AUC: 0.798
- Recall（離職クラス）: 0.49（class_weight="balanced"適用後、閾値0.30で運用）
- Feature importance上位: MonthlyIncome, DailyRate, Age, DistanceFromHome, MonthlyRate

### ファイル構成
```
hackathon_project/
├── app.py                        # Streamlitメインアプリ
├── src/
│   ├── model.py                  # transform_with_encoders(), validate_dataset(), friendly_data_error_message()
│   └── llm.py                    # explain_risk_factors(), suggest_interventions()
├── data/
│   ├── WA_Fn-UseC_-HR-Employee-Attrition.csv
│   └── company_policies.txt      # 簡易RAG用（6項目）
├── models/
│   ├── lgbm_model.pkl
│   ├── encoders.pkl
│   └── feature_names.pkl
├── design/
│   └── dashboard_reference.html  # Claude Designで作成した参考デザイン（不採用・アーカイブ扱い）
├── docs/
│   └── future_scope.md           # 将来構想の構想メモ（総合プロフィール分析／適性診断／チャットツール連携／代替データソースを統合予定）
└── README.md                     # デモ実行手順（v0.7で新規追加予定・提出物#3対応）
```

### 実装済み機能（MVP 5ステップ）
1. CSVアップロード・従業員選択（デモ用ショートカット: 高#622/中#1203/低#1804）
2. 離職リスクスコア表示（%、閾値0.30に対する判定）
3. Feature importance上位5件表示（原因分析セクションの寄与度チャートは削除方針、主要リスク要因の詳細表に一本化）
4. `explain_risk_factors()`：原因分析（自然文、日本語ラベル・円換算対応済み）
5. `suggest_interventions()`：簡易RAGベースの施策提案

### エラーハンドリング（8/23実装済み）
- API認証失敗・タイムアウト → `friendly_error_message()` でユーザー向けメッセージに変換
- CSV異常データ（欠損値・列欠落・型混入） → `validate_dataset()` + `friendly_data_error_message()` で保護
- `st.cache_resource`（モデル）/ `st.cache_data`（データ・LLM結果）でrerun時の重い処理再実行を回避
- 従業員ID別にsession_stateを管理し、切替時に古い分析結果が残らないようにしている

---

## 凍結タスク（将来構想として統合・実装見送り確定）

複数のTA提案（総合プロフィール分析、適性診断、チャットツール連携、データ制約への対応）を、**「データ取得のハードルを下げつつ、従業員を多面的に見る」という一つの将来ビジョンとして統合する。**

### 統合ストーリー（`docs/future_scope.md`記載予定）
1. **現状の課題**：本MVPはIBM HR Analytics（海外企業の詳細な人事データ）を使用しているが、日本企業の多くはここまで精緻な人事データを保有していない。
2. **代替データソースの方向性**：Slack等の社内チャットツールは日本企業でも導入率が高く、「新たに詳細データを集める」のではなく「既に存在するデータ（チャットログの頻度・トーン変化等）を使う」設計へ転換できる可能性がある。
3. **総合プロフィール分析への発展**：上記データを活用し、離職リスクという単一指標だけでなく、成長曲線・強み弱み・性質特質（適性診断的な要素を含む）を可視化する。

### 確定した対応
- **コード実装は行わない。**
- `docs/future_scope.md` に統合ストーリーとして記録し、プレゼンの「将来構想」スライドの参考資料とする。
- デモでは見せない。口頭・スライド上の言及に留める。

---

## 残りスケジュール（v0.7更新）

| 日付 | フェーズ | 内容 |
|------|---------|------|
| 8/26 | **UI修正（寄与度チャート削除・Markdownバグ修正）** | Claude Codeに依頼、デモ3パターンの動作確認必須 |
| 8/27 | **TAとのミーティング** | 新たな改善案を受領予定 |
| 8/27-28 | プレゼン資料最終化・台本作成 + **提出物準備開始** | PDF書き出し、README作成、サービス概要・AI技術一覧の文書化、デモ動画の構成検討 |
| 8/29 | リハーサル + **デモ動画撮影** | プレゼン本番のリハーサル（15分＝7分＋8分）、3分以内のデモ動画の撮影・編集 |
| 8/30 | 予備日 | 直前バグ修正のバッファ、提出物の最終確認 |
| 8/31 | **提出**（PDF・デモ動画・デモURL/実行方法・サービス概要・AI技術一覧） | — |

---

## 注意事項（Claude Codeへの申し送り）
- ユーザーはPython中心の学習歴で、フロントエンド（HTML/CSS/JavaScript）は基礎理解のみ。技術説明は平易に。
- Streamlit の制約（HTML/CSS直接編集不可、rerunで上書きされる）は理解済みなので、再度の説明は不要。
- **コード変更は「UI修正の対応状況」の#6・#7、および提出物対応の#3・#5に限定する。** それ以外の新規提案は着手前に必ずユーザーに確認する。
- デモの安定性（高・中・低リスク3パターンが正常動作すること）が最優先。これを壊すリスクのある変更は行わない。**8/29にデモ動画撮影を控えているため、それまでにアプリの状態を完全に固める。**
- 8/27にTAミーティングを控えているため、新たな指摘が来ても「完璧」より「安定動作＋主要指摘への対応実績」を優先する方針を維持する。
